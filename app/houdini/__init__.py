"""Houdini bridge and action runners.

This package will expose a typed action layer that executes structured
operations against a Houdini session (via ``hou`` inside ``hython`` or
through a long-lived RPC connection).
"""
